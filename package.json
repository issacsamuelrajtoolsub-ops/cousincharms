{
  "name": "cousincharms",
  "private": true,
  "workspaces": ["app-shared", "app-api", "app-web"],
  "scripts": {
    "build:shared": "cd app-shared && npm run build",
    "dev:web": "cd app-api && npm run start:local"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0"
  }
}
